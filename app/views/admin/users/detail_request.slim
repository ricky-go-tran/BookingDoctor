.user-management-wrapper
    .title User Management
    .controll-panel
        .left
            = link_to "Back", request_verify_admin_users_path, class: "back"
        .right
            = button_to "Accept", accepted_admin_users_path, method: :put, params: {id: @profile.id},class: "add"
            = button_to "Cancel", canceled_admin_users_path, method: :put,params: {id: @profile.id}, class: "pdf_export"
    .content-panel
        .own-info
            .title Infomation of own
            .fullname
                label Fullname:
                span = " #{@profile.fullname}"
            .birthday
                label  Birthday:
                span = @profile.birthday
            .address
                label  Address:
                span = @profile.address
        .clinic-info
            .title Information of clinic
            .name
                label  Name:
                span = @profile.clinic_profile.name
            .category
                label  Category:
                span = @category
            .address
                label  Address:
                span = @profile.clinic_profile.address
            .phone
                label  Phone:
                span = @profile.clinic_profile.phone
            .description
                label  Description:
                span = @profile.clinic_profile.description
            .active-time
                label Operating hours -
                label  Start:
                span = @profile.clinic_profile.start_hour
                label  End:
                span = @profile.clinic_profile.end_hour
            .active-time
                label Operating hours -
                label  Start:
                span = @start_day
                label  End:
                span = @end_day
            .certificate
                - if @profile.clinic_profile.certificate.attached?
                    = image_tag @profile.clinic_profile.certificate
                - else
                    label NO Image

