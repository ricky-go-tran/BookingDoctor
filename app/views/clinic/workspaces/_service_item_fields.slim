.nested-fields
    = f.collection_select :service_id, Service.where(clinic_profile_id: @old_medical.clinic_profile_id), :id, :name, include_blank: true
    = link_to_remove_association "Remove service", f
