.nested-fields
  = f.collection_select :service_id, Service.where(clinic_profile_id: @clinic.id), :id, :name, include_blank: true
  = link_to_remove_association "#{t('service.basic.service_remove')}", f
